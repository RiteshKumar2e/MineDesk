# ==========================================================================
# MineDesk frontend - example environment. Copy to .env and fill in the
# deployed backend's URL. Only VITE_* variables reach the browser bundle.
#   cp .env.example .env
# ==========================================================================

VITE_API_URL=http://127.0.0.1:4000
VITE_WS_URL=ws://127.0.0.1:4000
